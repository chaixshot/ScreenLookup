<Page
    x:Class="ScreenLookup.src.pages.SettingPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:Controls="clr-namespace:Wpf.Ui.Controls;assembly=Wpf.Ui"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:ScreenLookup.src.pages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:wpfui="http://schemas.lepo.co/wpfui/2022/xaml"
    xmlns:xControls="clr-namespace:ScreenLookup.src.controls"
    Title="Setting"
    mc:Ignorable="d">

    <Grid Margin="20,12,40,40">
        <Controls:DynamicScrollViewer>
            <StackPanel Grid.Column="0" Orientation="Vertical">
                <TextBlock
                    Margin="32,0,0,10"
                    FontSize="25"
                    FontWeight="SemiBold"
                    Text="Settings" />

                <TextBlock
                    Margin="5,0,0,5"
                    FontWeight="SemiBold"
                    Text="General" />

                <Controls:CardControl Margin="0,0,0,4" Icon="{wpfui:SymbolIcon Symbol=ClipboardTask24}">
                    <Controls:CardControl.Header>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="1" Margin="5,0,0,0">
                                <Controls:TextBlock Text="Startup With Windows" />
                                <wpfui:TextBlock
                                    Grid.Row="1"
                                    Foreground="{wpfui:ThemeResource TextFillColorSecondaryBrush}"
                                    Text="Auto start ScreenLookup when login" />
                            </StackPanel>
                        </Grid>
                    </Controls:CardControl.Header>
                    <CheckBox Grid.Column="1" IsChecked="{Binding StartupWithWindows}" />
                </Controls:CardControl>

                <Controls:CardControl Margin="0,0,0,4" Icon="{wpfui:SymbolIcon Symbol=SlideHide24}">
                    <Controls:CardControl.Header>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="1" Margin="5,0,0,0">
                                <Controls:TextBlock Text="Start In Background" />
                                <wpfui:TextBlock
                                    Grid.Row="1"
                                    Foreground="{wpfui:ThemeResource TextFillColorSecondaryBrush}"
                                    Text="Hide ScreenLook to tray after starting" />
                            </StackPanel>
                        </Grid>
                    </Controls:CardControl.Header>
                    <CheckBox Grid.Column="1" IsChecked="{Binding StartInBackground}" />
                </Controls:CardControl>

                <Controls:CardControl Margin="0,0,0,4" Icon="{wpfui:SymbolIcon Symbol=Apps24}">
                    <Controls:CardControl.Header>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="1" Margin="5,0,0,0">
                                <Controls:TextBlock Text="Minimize To Tray" />
                                <wpfui:TextBlock
                                    Grid.Row="1"
                                    Foreground="{wpfui:ThemeResource TextFillColorSecondaryBrush}"
                                    Text="Allow ScreenLookup hide to tray when minimizing and closing" />
                            </StackPanel>
                        </Grid>
                    </Controls:CardControl.Header>
                    <CheckBox Grid.Column="1" IsChecked="{Binding MinimizeToTray}" />
                </Controls:CardControl>

                <TextBlock
                    Margin="5,0,0,5"
                    FontWeight="SemiBold"
                    Text="Language" />

                <Controls:CardControl Margin="0,0,0,4" Icon="{wpfui:SymbolIcon Symbol=LocalLanguage24}">
                    <Controls:CardControl.Header>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="1" Margin="5,0,0,0">
                                <Controls:TextBlock Text="Source Language" />
                                <wpfui:TextBlock
                                    Grid.Row="1"
                                    Foreground="{wpfui:ThemeResource TextFillColorSecondaryBrush}"
                                    Text="Capture target language" />
                            </StackPanel>
                        </Grid>
                    </Controls:CardControl.Header>
                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                        <Controls:Button
                            x:Name="downloadTesseract"
                            Margin="0,0,5,0"
                            Click="DownloadTesseractButton_Click">
                            <StackPanel>
                                <xControls:LoadingSpiner x:Name="tesseractLoadingIcon" Width="12" />
                                <Controls:SymbolIcon
                                    x:Name="tesseractLoadIcon"
                                    Filled="True"
                                    Symbol="ArrowDownload24" />
                            </StackPanel>
                        </Controls:Button>
                        <ComboBox
                            x:Name="sourceLanguageAccuracy"
                            Grid.Column="1"
                            Width="110"
                            SelectedIndex="{Binding SourceLanguageAccuracy}" />
                        <ComboBox
                            x:Name="sourceLanguage"
                            Grid.Column="1"
                            Width="200"
                            Margin="5,0,0,0"
                            SelectedIndex="{Binding SourceLanguage}" />
                    </StackPanel>
                </Controls:CardControl>


                <Controls:CardControl
                    Margin="0,0,0,4"
                    Padding="14,8,14,8"
                    Icon="{wpfui:SymbolIcon Symbol=Checkmark24}">
                    <Controls:CardControl.Header>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="1" Margin="5,0,0,0">
                                <Controls:TextBlock Text="Source Language Correction (Hunspell)" />
                                <wpfui:TextBlock
                                    Grid.Row="1"
                                    Foreground="{wpfui:ThemeResource TextFillColorSecondaryBrush}"
                                    Text="Replace the words that are similar to recognized ones in dictionary" />
                                <wpfui:TextBlock Grid.Row="2">
                                    <Hyperlink NavigateUri="https://hunspell.github.io/" RequestNavigate="Hyperlink_RequestNavigate">
                                        Read more
                                    </Hyperlink>
                                </wpfui:TextBlock>
                            </StackPanel>
                        </Grid>
                    </Controls:CardControl.Header>
                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                        <Controls:Button
                            x:Name="downloadHunspell"
                            Margin="0,0,5,0"
                            Click="DownloadHunspellButton_Click">
                            <StackPanel>
                                <xControls:LoadingSpiner x:Name="hunspellLoadingIcon" Width="12" />
                                <Controls:SymbolIcon
                                    x:Name="hunspellLoadIcon"
                                    Filled="True"
                                    Symbol="ArrowDownload24" />
                            </StackPanel>
                        </Controls:Button>
                        <CheckBox IsChecked="{Binding HunSpell}" />
                    </StackPanel>
                </Controls:CardControl>


                <Controls:CardControl Margin="0,0,0,4" Icon="{wpfui:SymbolIcon Symbol=LocalLanguage24}">
                    <Controls:CardControl.Header>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="1" Margin="5,0,0,0">
                                <Controls:TextBlock Text="Target Language" />
                                <wpfui:TextBlock
                                    Grid.Row="1"
                                    Foreground="{wpfui:ThemeResource TextFillColorSecondaryBrush}"
                                    Text="Language for translating to you native language" />
                            </StackPanel>
                        </Grid>
                    </Controls:CardControl.Header>
                    <ComboBox
                        x:Name="targetLanguage"
                        Grid.Column="1"
                        Width="200"
                        SelectedIndex="{Binding TargetLanguage}" />
                </Controls:CardControl>

                <Controls:CardControl Margin="0,0,0,4" Icon="{wpfui:SymbolIcon Symbol=Translate24}">
                    <Controls:CardControl.Header>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="1" Margin="5,0,0,0">
                                <Controls:TextBlock Text="Translation Provider" />
                                <wpfui:TextBlock
                                    Grid.Row="1"
                                    Foreground="{wpfui:ThemeResource TextFillColorSecondaryBrush}"
                                    Text="Provider for translation service" />
                            </StackPanel>
                        </Grid>
                    </Controls:CardControl.Header>
                    <ComboBox
                        x:Name="translationProvider"
                        Grid.Column="1"
                        Width="200"
                        SelectedIndex="{Binding TranslationProvider}" />
                </Controls:CardControl>

                <Controls:CardControl Margin="0,0,0,4" Icon="{wpfui:SymbolIcon Symbol=PersonVoice24}">
                    <Controls:CardControl.Header>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="1" Margin="5,0,0,0">
                                <Controls:TextBlock Text="Text-To-Speech Provider" />
                                <wpfui:TextBlock
                                    Grid.Row="1"
                                    Foreground="{wpfui:ThemeResource TextFillColorSecondaryBrush}"
                                    Text="Provider for Text-To-Speech service" />
                            </StackPanel>
                        </Grid>
                    </Controls:CardControl.Header>
                    <ComboBox
                        x:Name="ttsProvider"
                        Grid.Column="1"
                        Width="200"
                        SelectedIndex="{Binding TTSProvider}" />
                </Controls:CardControl>

                <TextBlock
                    Margin="5,0,0,5"
                    FontWeight="SemiBold"
                    Text="Lookup" />

                <Controls:CardControl Margin="0,0,0,4" Icon="{wpfui:SymbolIcon Symbol=Keyboard24}">
                    <Controls:CardControl.Header>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="1" Margin="5,0,0,0">
                                <Controls:TextBlock Text="Lookup Shortcut" />
                                <wpfui:TextBlock
                                    Grid.Row="1"
                                    Foreground="{wpfui:ThemeResource TextFillColorSecondaryBrush}"
                                    Text="Hotkey for start Lookup the screen" />
                            </StackPanel>
                        </Grid>
                    </Controls:CardControl.Header>
                    <xControls:ShortcutControl
                        x:Name="captureShortcut"
                        Grid.Column="1"
                        KeySetChanged="ShortcutControl_KeySetChanged" />
                </Controls:CardControl>

                <Controls:CardControl Margin="0,0,0,4" Icon="{wpfui:SymbolIcon Symbol=Image24}">
                    <Controls:CardControl.Header>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="1" Margin="5,0,0,0">
                                <Controls:TextBlock Text="Show Capture Image" />
                                <wpfui:TextBlock
                                    Grid.Row="1"
                                    Foreground="{wpfui:ThemeResource TextFillColorSecondaryBrush}"
                                    Text="Show Lookup's result capture image" />
                            </StackPanel>
                        </Grid>
                    </Controls:CardControl.Header>
                    <CheckBox Grid.Column="1" IsChecked="{Binding ShowImage}" />
                </Controls:CardControl>

                <Controls:CardControl Margin="0,0,0,4" Icon="{wpfui:SymbolIcon Symbol=Highlight24}">
                    <Controls:CardControl.Header>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="1" Margin="5,0,0,0">
                                <Controls:TextBlock Text="Show Word Highlight" />
                                <wpfui:TextBlock
                                    Grid.Row="1"
                                    Foreground="{wpfui:ThemeResource TextFillColorSecondaryBrush}"
                                    Text="Show Lookup's result source language words highlighting" />
                            </StackPanel>
                        </Grid>
                    </Controls:CardControl.Header>
                    <CheckBox Grid.Column="1" IsChecked="{Binding ShowHighlight}" />
                </Controls:CardControl>

                <Controls:CardControl Margin="0,0,0,4" Icon="{wpfui:SymbolIcon Symbol=Window24}">
                    <Controls:CardControl.Header>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="1" Margin="5,0,0,0">
                                <Controls:TextBlock Text="Close When Lost Focus" />
                                <wpfui:TextBlock
                                    Grid.Row="1"
                                    Foreground="{wpfui:ThemeResource TextFillColorSecondaryBrush}"
                                    Text="Automatically close result window when losing focus" />
                            </StackPanel>
                        </Grid>
                    </Controls:CardControl.Header>
                    <CheckBox Grid.Column="1" IsChecked="{Binding CloseLostFocus}" />
                </Controls:CardControl>

                <Controls:CardControl Margin="0,0,0,4" Icon="{wpfui:SymbolIcon TextFontSize24}">
                    <Controls:CardControl.Header>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="1" Margin="5,0,0,0">
                                <Controls:TextBlock Text="Font" />
                                <wpfui:TextBlock
                                    Grid.Row="1"
                                    Foreground="{wpfui:ThemeResource TextFillColorSecondaryBrush}"
                                    Text="Lookup's result font style" />
                            </StackPanel>
                        </Grid>
                    </Controls:CardControl.Header>
                    <ComboBox
                        Grid.Column="1"
                        Width="200"
                        FontFamily="{Binding FontFace}"
                        IsEditable="True"
                        ItemsSource="{Binding Source={x:Static Member=Fonts.SystemFontFamilies}}"
                        Text="{Binding FontFace}">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <Label Content="{Binding Source}" FontFamily="{Binding .}" />
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>
                </Controls:CardControl>

                <Controls:CardControl Margin="0,0,0,4" Icon="{wpfui:SymbolIcon TextFontSize24}">
                    <Controls:CardControl.Header>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="1" Margin="5,0,0,0">
                                <Controls:TextBlock Text="Font Size" />
                                <wpfui:TextBlock
                                    Grid.Row="1"
                                    Foreground="{wpfui:ThemeResource TextFillColorSecondaryBrush}"
                                    Text="Lookup's result font size" />
                            </StackPanel>
                        </Grid>
                    </Controls:CardControl.Header>
                    <Controls:NumberBox
                        Grid.Column="1"
                        Width="200"
                        Maximum="100"
                        Minimum="10"
                        Value="{Binding FontSizeS, UpdateSourceTrigger=PropertyChanged}" />
                </Controls:CardControl>

                <TextBlock
                    Margin="5,0,0,5"
                    FontWeight="SemiBold"
                    Text="Reset" />

                <Controls:Card Margin="0,0,0,4">
                    <Controls:Button
                        Margin="5,0,0,0"
                        HorizontalAlignment="Left"
                        Click="ResetButton"
                        Icon="{wpfui:SymbolIcon Symbol=ArrowCounterclockwise24,
                                                Filled=False}"
                        ToolTip="Reset" />
                </Controls:Card>

            </StackPanel>
        </Controls:DynamicScrollViewer>
    </Grid>
</Page>
