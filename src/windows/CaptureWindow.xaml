<ui:FluentWindow
    x:Class="ScreenLookup.src.windows.CaptureWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="clr-namespace:ScreenLookup.src.windows"
    mc:Ignorable="d"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="Capture"
        
    ResizeMode="CanResize"
    ShowInTaskbar="False"
    Topmost="True"
    WindowStyle="None"
    SizeToContent="Height"
    WindowStartupLocation="CenterScreen"
    Deactivated="App_Deactivated"
    >
    <Grid Margin="10,5,10,5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <ui:Card Grid.Row="0" Padding="0">
            <ui:Image x:Name="captureImage" Stretch="None"/>
        </ui:Card>

        <ui:Card Grid.Row="1" Margin="0,3,0,0" Visibility="Collapsed">
            <ui:DynamicScrollViewer>
                <ui:TextBlock x:Name="originalText" TextWrapping="Wrap" Text="originalText" Grid.Column="1" Margin="45,0,0,0"/>
            </ui:DynamicScrollViewer>
        </ui:Card>

        <ui:Card x:Name="originalWords" Grid.Row="2" Margin="0,3,0,0">
            <ui:DynamicScrollViewer>
                <Grid >
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <ui:Button
                HorizontalContentAlignment="Center"
                VerticalContentAlignment="Center"
                Click="Button_OriginalTTS"
                Icon="{ui:SymbolIcon Speaker228,
                    Filled=True}" VerticalAlignment="Top"/>


                    <StackPanel Grid.Column="1" Margin="5,0,0,0">
                        <ItemsControl Name="ocrWords">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel Orientation="Horizontal"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>

                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <ui:Button
                                    BorderThickness="{Binding Border}"
                                    Content="{Binding Word}"
                                    HorizontalAlignment="Left"
                                    Click="Button_Word"
                                    Margin="0,0,0,0"
                                    Padding="1.5,0,1.5,0"
                                    Width="{Binding Width}" 
                                    Height="{Binding Height}" 
                                    />
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Grid>
            </ui:DynamicScrollViewer>
        </ui:Card>

        <ui:Card x:Name="definition" Grid.Row="3" Margin="0,3,0,0" >
            <ui:DynamicScrollViewer>
                <StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <ui:Button
                        HorizontalContentAlignment="Center"
                        VerticalContentAlignment="Center"
                        Click="Button_WordOriginalTTS"
                        Icon="{ui:SymbolIcon Speaker228,
                            Filled=True}" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        <ui:TextBlock x:Name="definition_original" Text="11111" Margin="5,0,0,0" VerticalAlignment="Center" HorizontalAlignment="Center" />
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,5,0,0">
                        <ui:Button
                         HorizontalContentAlignment="Center"
                         VerticalContentAlignment="Center"
                         Click="Button_WordTranslatedTTS"
                         Icon="{ui:SymbolIcon Speaker228,
                             Filled=True}" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        <ui:TextBlock x:Name="definition_translated" Text="222222" Margin="5,0,0,0" HorizontalAlignment="Center" VerticalAlignment="Center" />

                    </StackPanel>
                </StackPanel>
            </ui:DynamicScrollViewer>
        </ui:Card>

        <ui:Card x:Name="translatedCard" Grid.Row="4" Margin="0,3,0,0">
            <ui:DynamicScrollViewer >
                <Grid >
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <ui:Button
                    HorizontalContentAlignment="Center"
                    VerticalContentAlignment="Center"
                    Click="Button_TranslatedTTS"
                    Icon="{ui:SymbolIcon Speaker228,
                        Filled=True}" VerticalAlignment="Top"/>
                    <ui:TextBlock x:Name="translatedText" TextWrapping="Wrap" Text="translatedText" Grid.Column="1" Margin="5,0,0,0"/>
                </Grid>
            </ui:DynamicScrollViewer>
        </ui:Card>

    </Grid>
</ui:FluentWindow>
